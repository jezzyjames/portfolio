import 'package:flutter/material.dart';

class EPODScreen extends StatelessWidget {
  const EPODScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Epod");
  }
}